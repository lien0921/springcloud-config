config:
    info: master branch, springcloud-config/config-dev.yml version=7

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource       #當前數據源操作類型
    driver-class-name: com.mysql.jdbc.Driver        #mysql驅動包
    url: jdbc:mysql://localhost:3306/mycompany?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: jeff
    password: 660921

  jpa:
     hibernate:
        ddl-auto: update
     show-sql: true
